% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-phase-space.R
\name{saomnk_plot_phase_evolution}
\alias{saomnk_plot_phase_evolution}
\title{Phase Space Evolution Animation Frames}
\usage{
saomnk_plot_phase_evolution(
  env,
  x_var = "K_AC",
  y_var = "utility",
  z_var = "exploration_rate",
  trail_length = 10,
  fps = 5,
  thin_factor = 5,
  use_3d = FALSE
)
}
\arguments{
\item{env}{SaomNkRSienaBiEnv object after simulation.}

\item{x_var}{First dimension variable.}

\item{y_var}{Second dimension variable.}

\item{z_var}{Third dimension variable (used only if \pkg{plotly} is available
and \code{use_3d = TRUE}; otherwise the 2D projection is used).}

\item{trail_length}{Number of past steps to include as trajectory trail
behind each actor's current position (default 10).}

\item{fps}{Frames per second for the animation (default 5).}

\item{thin_factor}{Integer thinning factor for chain steps.}

\item{use_3d}{Logical; attempt 3D plotly animation if \code{TRUE} and
\pkg{plotly} is available (default \code{FALSE} -- uses gganimate).}
}
\value{
A \code{gganimate} object (if \pkg{gganimate} is available),
otherwise a list of \code{ggplot} frame objects.
}
\description{
Generate frame-by-frame data for animating system evolution through
phase space. Each frame shows actor positions at one timestep with
trailing trajectories from previous steps.
}
