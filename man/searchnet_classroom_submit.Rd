% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/searchnet-teaching.R
\name{searchnet_classroom_submit}
\alias{searchnet_classroom_submit}
\title{Submit a Student Decision for One Round}
\usage{
searchnet_classroom_submit(classroom, student_id, adds = NULL, drops = NULL)
}
\arguments{
\item{classroom}{A \code{searchnet_classroom} object from
\code{\link{searchnet_classroom_init}}.}

\item{student_id}{Character. Student identifier (e.g., "student_1").}

\item{adds}{Integer vector of activity IDs (column indices) to add.
These must be activities the student's firm does not currently hold.}

\item{drops}{Integer vector of activity IDs (column indices) to drop.
These must be activities the student's firm currently holds.}
}
\value{
The updated classroom object (invisibly). Prints a confirmation
message and the number of students still pending.
}
\description{
Records the strategic decisions made by a student for the current round.
The student specifies which activities to add to their portfolio and which
to drop. Once all students have submitted, the round can be advanced.
}
\examples{
\dontrun{
session <- searchnet_classroom_submit(
  session, "student_1", adds = c(3, 7), drops = c(1)
)
}
}
