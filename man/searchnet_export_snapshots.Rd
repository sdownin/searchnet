% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/searchnet-export.R
\name{searchnet_export_snapshots}
\alias{searchnet_export_snapshots}
\title{Export bipartite matrix snapshots to CSV}
\usage{
searchnet_export_snapshots(
  env,
  steps = NULL,
  file = "snapshots.csv",
  sparse = TRUE
)
}
\arguments{
\item{env}{A \code{SaomNkRSienaBiEnv} object after simulation.}

\item{steps}{Integer vector of chain step indices to export. If \code{NULL}
(default), selects up to 20 evenly spaced steps across the full chain.}

\item{file}{Output CSV file path.}

\item{sparse}{Logical; if \code{TRUE} (default), only writes rows where
\code{tie == 1}, substantially reducing file size for sparse networks.}
}
\value{
Invisible path to the written file.
}
\description{
For selected chain steps, writes the M x N bipartite incidence matrix in
long format. This is useful for rendering network graphs in manim or other
visualization tools.
}
\details{
Output columns:
\describe{
\item{step}{Chain step id}
\item{actor_id}{Row index (actor, 1-based)}
\item{component_id}{Column index (component, 1-based)}
\item{tie}{0 or 1 indicating whether a tie exists}
}
}
\examples{
\donttest{
env <- saomnk_env(M = 4, N = 6, seed = 42)
mod <- saomnk_model(density = -0.5, popularity = 0.2)
saomnk_run(env, mod, steps_per_actor = 5, seed = 12345)

f <- tempfile(fileext = ".csv")
searchnet_export_snapshots(env, steps = c(1, 10, 20), file = f)
head(read.csv(f))
unlink(f)
}
}
