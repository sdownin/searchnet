% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/searchnet-export.R
\name{searchnet_export_utility}
\alias{searchnet_export_utility}
\title{Export actor utility trajectories to CSV}
\usage{
searchnet_export_utility(env, file = "utility_trajectory.csv")
}
\arguments{
\item{env}{A \code{SaomNkRSienaBiEnv} object after simulation.}

\item{file}{Output CSV file path for actor-level data. Summary file is
written with \code{_summary} appended before the extension.}
}
\value{
Invisible character vector of written file paths.
}
\description{
Extracts the actor utility data frame computed during the simulation chain
and writes it to CSV. Includes both the raw actor-level data and a per-step
summary.
}
\details{
Actor-level output columns:
\describe{
\item{step}{Chain step id}
\item{actor_id}{Actor id}
\item{utility}{Total utility value}
\item{strategy}{Actor strategy label}
\item{stability}{Whether this was a no-change step}
}

Summary output columns:
\describe{
\item{step}{Chain step id}
\item{mean_utility}{Mean utility across actors}
\item{sd_utility}{Standard deviation}
\item{min_utility}{Minimum}
\item{max_utility}{Maximum}
}
}
\examples{
\donttest{
env <- saomnk_env(M = 4, N = 6, seed = 42)
mod <- saomnk_model(density = -0.5, popularity = 0.2)
saomnk_run(env, mod, steps_per_actor = 5, seed = 12345)

f <- file.path(tempdir(), "utility_trajectory.csv")
files <- searchnet_export_utility(env, file = f)
head(read.csv(files[["summary"]]))
unlink(files)
}
}
