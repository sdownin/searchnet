% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/saomnk-diagnostics.R
\name{plot.saomnk_sai}
\alias{plot.saomnk_sai}
\title{Plot method for saomnk_sai objects}
\usage{
\method{plot}{saomnk_sai}(x, type = c("curve", "tile", "forest"), effects = NULL, ...)
}
\arguments{
\item{x}{An object of class \code{"saomnk_sai"}.}

\item{type}{Character. One of \code{"curve"} (specification curve with CI),
\code{"tile"} (sign/significance heatmap across specs), or
\code{"forest"} (forest plot of median estimates with SAI scores).}

\item{effects}{Character vector or NULL. Subset of effects to plot.}

\item{...}{Additional arguments passed to ggplot2.}
}
\value{
A ggplot2 object.
}
\description{
Produces one of three visualization types for specification curve analysis.
}
\examples{
\donttest{
grid <- data.frame(
  effect = rep(c("reciprocity", "transitivity"), each = 5),
  specification = rep(paste0("spec_", 1:5), 2),
  estimate = c(0.8, 0.9, 0.7, 0.85, 0.75,
               0.3, -0.1, 0.2, 0.4, -0.05),
  std_error = rep(0.15, 10)
)
result <- saomnk_sai(grid)

p1 <- plot(result, type = "curve")
p2 <- plot(result, type = "tile")
p3 <- plot(result, type = "forest")
}
}
