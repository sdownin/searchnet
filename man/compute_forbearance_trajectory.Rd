% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/track-entries.R
\name{compute_forbearance_trajectory}
\alias{compute_forbearance_trajectory}
\title{Compute competitive entry rate over time (rolling window)}
\usage{
compute_forbearance_trajectory(entry_log, window = 50L)
}
\arguments{
\item{entry_log}{A \code{data.frame} produced by \code{track_entry_decisions()}.}

\item{window}{Integer. Number of consecutive ADD events in the rolling
window. Default 50.}
}
\value{
A \code{data.frame} with columns \code{step}, \code{window_center},
\code{competitive_entry_rate}, \code{mean_rivals_at_entry}.
}
\description{
Useful for plotting how forbearance evolves across the simulation.
}
\examples{
set.seed(7)
h <- list(matrix(rbinom(12, 1, 0.4), nrow = 3))
for (t in 2:16) {
  m <- h[[t - 1]]
  i <- sample(3, 1); j <- sample(4, 1)
  m[i, j] <- 1 - m[i, j]
  h[[t]] <- m
}
entry_log <- track_entry_decisions(holdings_history = h)
compute_forbearance_trajectory(entry_log, window = 3)
}
