% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/searchnet-imitation.R
\name{saomnk_env_imitation}
\alias{saomnk_env_imitation}
\title{Imitation statistic for a searchnet environment}
\usage{
saomnk_env_imitation(env, performance = NULL, step = NULL, ...)
}
\arguments{
\item{env}{A \code{SaomNkRSienaBiEnv}.}

\item{performance}{Optional numeric vector of length M. When omitted, actor
scope (row sums of the bipartite matrix) is used as the performance proxy,
and a message says so, because scope is a poor stand-in for fitness and a
silent default here would be the kind of substitution that makes results
hard to trace.}

\item{step}{Optional step index into the environment's state array; the
current state is used when omitted.}

\item{...}{Passed to \code{\link{saomnk_sim_ego_indist2}}.}
}
\value{
Numeric vector of length M, or a matrix, per \code{per_component}.
}
\description{
Convenience wrapper that pulls the bipartite state and a performance vector
out of a \code{SaomNkRSienaBiEnv} and applies
\code{\link{saomnk_sim_ego_indist2}}.
}
\examples{
env <- saomnk_env(M = 5, N = 8, density = 0.3, seed = 1)
saomnk_env_imitation(env, performance = runif(5))
}
