% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nk-classic.R
\name{nk_sweep_K}
\alias{nk_sweep_K}
\title{Sweep landscape statistics across K}
\usage{
nk_sweep_K(
  N = 12,
  K_values = 0:(N - 1),
  n_landscapes = 10,
  n_walks = 20,
  model = c("adjacent", "random", "block"),
  seed = NULL
)
}
\arguments{
\item{N}{Integer. Number of loci.}

\item{K_values}{Integer vector of \eqn{K} levels to sweep.}

\item{n_landscapes}{Integer. Replicate landscapes per \eqn{K}.}

\item{n_walks}{Integer. Adaptive walks per landscape (random starts).}

\item{model}{Character. Neighbourhood topology.}

\item{seed}{Integer or \code{NULL}.}
}
\value{
A \code{data.frame} with one row per \eqn{K}, containing mean local
optima count, mean walk length, mean terminal fitness, and the global
optimum fitness.
}
\description{
Replicates the canonical NK ruggedness experiment: as \eqn{K} rises, the
number of local optima grows (roughly \eqn{2^N / (K+1)}) and adaptive walks
terminate sooner at lower-fitness peaks.
}
\examples{
\donttest{
nk_sweep_K(N = 10, K_values = c(0, 2, 4, 8), n_landscapes = 3)
}

}
