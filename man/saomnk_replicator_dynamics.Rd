% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/searchnet-replicator.R
\name{saomnk_replicator_dynamics}
\alias{saomnk_replicator_dynamics}
\title{Run Replicator Dynamics on Policy Type Populations}
\usage{
saomnk_replicator_dynamics(
  fitness_matrix,
  population = NULL,
  generations = 500L,
  selection_strength = 200,
  policy_labels = c("RS", "RE", "RR", "RF")
)
}
\arguments{
\item{fitness_matrix}{Matrix of fitness values. Rows = policy types,
columns = replications or conditions. If a named vector, treated as
a single condition.}

\item{population}{Numeric vector of initial population shares (must sum to 1).
If NULL, starts with equal shares.}

\item{generations}{Integer number of generations to simulate (default 500).}

\item{selection_strength}{Numeric selection intensity (default 200).
Higher values = stronger selection pressure.}

\item{policy_labels}{Character vector of policy type names.}
}
\value{
Data frame with columns: generation, policy_type, share, fitness
}
\description{
Implements discrete-time replicator dynamics with softmax selection.
The replicator equation: x_i(t+1) = x_i(t) * exp(s * f_i) / Z
where s is selection strength and Z is the normalization constant.
}
\examples{
## Near-parity fitness with a slight RF advantage
traj <- saomnk_replicator_dynamics(
  c(RS = 0.80, RE = 0.81, RR = 0.79, RF = 0.82),
  generations = 100
)
tail(traj, 4)   # terminal population shares
}
