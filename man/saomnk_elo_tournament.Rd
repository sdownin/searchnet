% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/searchnet-replicator.R
\name{saomnk_elo_tournament}
\alias{saomnk_elo_tournament}
\title{Run ELO Tournament Between Policy Types}
\usage{
saomnk_elo_tournament(
  fitness_by_type,
  matches_per_pair = 60L,
  K_elo = 32,
  initial_rating = 1500,
  policy_labels = names(fitness_by_type)
)
}
\arguments{
\item{fitness_by_type}{Named list of fitness vectors, one per policy type.
Each vector contains fitness outcomes from multiple replications.}

\item{matches_per_pair}{Integer number of matches per pairwise comparison
(default 60).}

\item{K_elo}{Numeric ELO update constant (default 32).}

\item{initial_rating}{Numeric starting ELO rating (default 1500).}

\item{policy_labels}{Character vector of policy type labels.}
}
\value{
List with components:
\item{ratings}{Named numeric vector of final ELO ratings}
\item{history}{Data frame of match results}
\item{parity_test}{Logical: are all ratings within 20 points?}
}
\description{
Runs a round-robin ELO tournament where policy types compete
head-to-head based on fitness outcomes from simulation.
}
\examples{
set.seed(42)
fitness_by_type <- list(
  RS = rnorm(30, 0.80, 0.05),
  RE = rnorm(30, 0.80, 0.05),
  RR = rnorm(30, 0.80, 0.05),
  RF = rnorm(30, 0.80, 0.05)
)
tour <- saomnk_elo_tournament(fitness_by_type, matches_per_pair = 10)
tour$ratings
tour$parity_test
}
