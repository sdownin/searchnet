% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/saomnk-api.R
\name{saomnk_monte_carlo}
\alias{saomnk_monte_carlo}
\title{Run Parallel Monte Carlo Replications}
\usage{
saomnk_monte_carlo(
  env,
  model,
  replications = 10,
  waves = 2,
  iterations = 500,
  seed = 12345,
  parallel = FALSE,
  workers = NULL
)
}
\arguments{
\item{env}{A \code{SaomNkRSienaBiEnv} object created by
\code{\link{saomnk_env}}.}

\item{model}{A structure model created by \code{\link{saomnk_model}}.}

\item{replications}{Integer. Number of independent replications
(default \code{10}).}

\item{waves}{Integer. Number of waves per replication (default \code{2}).}

\item{iterations}{Integer. RSiena iterations per wave (default \code{500}).}

\item{seed}{Integer. Base random seed; replication \emph{r} uses
\code{seed + r}.}

\item{parallel}{Logical. If \code{TRUE}, run replications in parallel
using the \pkg{future} back-end (default \code{FALSE}).}

\item{workers}{Integer or \code{NULL}. Number of parallel workers.
If \code{NULL}, uses the current \pkg{future} plan.}
}
\value{
The \code{env} object (modified in place) with
\code{env$mc_results} populated; returned invisibly.
}
\description{
Executes independent replications of a SaoMNK multiwave simulation, each
starting from the same initial bipartite matrix but with a different random
seed.  When \code{parallel = TRUE} and the \pkg{future} / \pkg{future.apply}
packages are available, replications run across multiple R worker processes.
}
\examples{
\dontrun{
env <- saomnk_env(M = 6, N = 12, seed = 42)
mod <- saomnk_model(density = -0.5,
                    influence_matrix = saomnk_block_diagonal(12, 4))
saomnk_monte_carlo(env, mod, replications = 20, parallel = TRUE, workers = 4)
}
}
