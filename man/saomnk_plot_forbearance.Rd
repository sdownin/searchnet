% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-forbearance.R
\name{saomnk_plot_forbearance}
\alias{saomnk_plot_forbearance}
\title{Standalone forbearance plot functions for SaoMNK.}
\usage{
saomnk_plot_forbearance(
  env = NULL,
  entry_log = NULL,
  window = 50L,
  show_density = FALSE,
  loess_span = 0.4,
  plot_return = TRUE
)
}
\arguments{
\item{env}{SaomNkRSienaBiEnv object with a completed simulation, or
\code{NULL} if \code{entry_log} is supplied directly.}

\item{entry_log}{Optional data.frame from \code{track_entry_decisions()}.
If \code{NULL}, computed from \code{env}.}

\item{window}{Integer rolling-window size for smoothing. Default 50.}

\item{show_density}{Logical. If TRUE, overlay a marginal density on the
right axis showing rivals-at-entry distribution. Default FALSE.}

\item{loess_span}{Numeric span for loess smoother. Default 0.4.}

\item{plot_return}{Logical; if TRUE return the ggplot object. Default TRUE.}
}
\value{
A ggplot object (if \code{plot_return} is TRUE).
}
\description{
Visualisations of competitive entry/exit behaviour derived from the
entry tracking in \code{track-entries.R}. Each function follows the
package convention: takes an \code{env} (SaomNkRSienaBiEnv) object or
a pre-computed data.frame as its first argument.
Plot competitive entry rate over the simulation chain
}
\details{
Shows a rolling-window competitive entry rate (proportion of ADD actions
directed at rival-occupied activities), optionally grouped by actor
strategy.
}
\examples{
\donttest{
## From a synthetic holdings history (no simulation required)
set.seed(7)
h <- list(matrix(rbinom(12, 1, 0.4), nrow = 3))
for (t in 2:30) {
  m <- h[[t - 1]]
  i <- sample(3, 1); j <- sample(4, 1)
  m[i, j] <- 1 - m[i, j]
  h[[t]] <- m
}
entry_log <- track_entry_decisions(holdings_history = h)
p <- saomnk_plot_forbearance(entry_log = entry_log, window = 5)
}
}
