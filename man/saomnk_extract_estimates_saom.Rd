% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/saomnk-diagnostics.R
\name{saomnk_extract_estimates_saom}
\alias{saomnk_extract_estimates_saom}
\title{Extract estimates from a sienaFit object}
\usage{
saomnk_extract_estimates_saom(
  fit,
  specification = "saom",
  include_rate = FALSE
)
}
\arguments{
\item{fit}{A \code{sienaFit} object from \code{RSiena::siena07()}.}

\item{specification}{Character. A label for this specification (used as the
\code{specification} column in the output).}

\item{include_rate}{Logical. Whether to include rate parameters (default
FALSE).}
}
\value{
A data.frame with columns: effect, specification, estimate,
std_error, convergence_t.
}
\description{
Pulls effect names, estimates, standard errors, and convergence diagnostics
from an RSiena model fit into a tidy data.frame suitable for
\code{\link{saomnk_sai}}.
}
\examples{
\donttest{
## Quick fit on RSiena's built-in s50 friendship panel
friendship <- RSiena::sienaDependent(
  array(c(RSiena::s501, RSiena::s502), dim = c(50, 50, 2))
)
dat <- RSiena::sienaDataCreate(friendship)
eff <- RSiena::getEffects(dat)
alg <- RSiena::sienaAlgorithmCreate(projname = NULL, nsub = 1, n3 = 50,
                                    seed = 7)
fit <- RSiena::siena07(alg, data = dat, effects = eff,
                       batch = TRUE, silent = TRUE)

saomnk_extract_estimates_saom(fit, specification = "baseline")
}
}
